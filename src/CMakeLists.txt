# This will contain lots of files like the following
# add_experiment(executable_name sources...)

# I also suggest adding a library for the core source files
# so that you potentially don't have to recompile it so often.
